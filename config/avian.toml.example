# AVIAN configuration schema. Unknown keys are rejected.
schema_version = 1

[node]
name = "change-me"
role = "observer"

[peat]
bind = "0.0.0.0:9000"
storage = "/var/lib/avian"
formation_id = "change-me"
formation_key_file = "formation.key"
max_mesh_peers = 4
peer_retry_seconds = 5

[sockets]
control = "/run/avian/control.sock"
payload = "/run/avian/payload-events.sock"
link_observation = "/run/avian/link-observations.sock"
max_message_bytes = 65536

[commands]
mode = "disabled"
environment = "hardware"
state_file = "command-state.json"
lifetime_ms = 5000
poll_ms = 250
ack_timeout_ms = 1500
retries = 1

[radio]
enabled = false
observation_interval_seconds = 10
probe_timeout_ms = 1000
